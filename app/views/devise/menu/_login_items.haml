%ul.hmenu
  - if member_signed_in?
    %li
      Logged in as
      = link_to current_member.email, edit_member_path(current_member)
    %li= link_to "Logout", destroy_member_session_path, method: :delete
  - else
    %li= link_to "Login", new_member_session_path
