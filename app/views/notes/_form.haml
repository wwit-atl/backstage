- if can? :create, Note
  = simple_form_for [@notable, @note] do |f|
    - if @note.errors.any?
      = render 'partials/form_errors', model: @note

    .field= f.input :content, label: false
    .actions= f.submit class: 'btn btn-default'
