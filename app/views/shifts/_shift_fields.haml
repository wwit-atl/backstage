.field
  = f.association :skill, collection: @skills_crewable, include_blank: false, label: false,
    :wrapper_html => { :style => 'display: inline' }
  = f.association :member, collection: @members_crewable, include_blank: true, label: false,
    :wrapper_html => { :style => 'display: inline' }
  = f.hidden_field :_destroy
  = link_to button_tag('Delete Shift'), '#', class: 'remove_fields'
