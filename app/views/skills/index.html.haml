%h1 Listing skills

- @categories.each do |category|
  %h2= category
  %table
    %tr
      %th Code
      %th Name
      %th Description
      %th Training?
      %th Ranked?
      %th
      %th
      %th

    - @skills.each do |skill|
      - if skill.category == category
        %tr
          %td= skill.code
          %td= skill.name
          %td= skill.description
          %td= skill.training? ? 'Yes' : 'No'
          %td= skill.ranked? ? 'Yes' : 'No'
          -#%td= link_to 'Show', skill
          %td= link_to 'Edit', edit_skill_path(skill)
          %td= link_to 'Destroy', skill, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br= link_to 'New Skill', new_skill_path
